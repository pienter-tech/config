return {
    {
        "olimorris/persisted.nvim",
        opts = {
            autosave = true,
            autoload = true,
            allowed_dirs = {
                "~/dev/projects"
            }
        },
    }
}
